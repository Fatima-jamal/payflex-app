# PostgreSQL RDS direct connection (no SSH tunnel)
spring.datasource.url=jdbc:postgresql://<RDS-ENDPOINT>:5432/postgres
spring.datasource.username=postgresadmin
spring.datasource.password=StrongPassword123!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JSP Resolver
spring.mvc.view.prefix=/WEB-INF/pages/
spring.mvc.view.suffix=.jsp

# Server Port
server.port=8081# PostgreSQL RDS direct connection (no SSH tunnel)
spring.datasource.url=jdbc:postgresql://postgres-db.cfoaacgcm9hg.us-west-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgresadmin
spring.datasource.password=StrongPassword123!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JSP Resolver
spring.mvc.view.prefix=/WEB-INF/pages/
spring.mvc.view.suffix=.jsp

# Server Port
server.port=8081

# View mapping and error handling
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true


# View mapping and error handling
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true
